# Scripts

To hack on the scripts, `cd` to this directory, then

- Use `prek/pre-commit run -a jsonschema-gentypes` to generate types from schema initially
- Use `prek/pre-commit install` to run checks on commit
